/**
 * Placeholder. The renderer (React + Vite single-file bundle) is build step 3
 * — assumption ledger only — per DESIGN.html §9. Nothing here yet.
 */
export const NOT_IMPLEMENTED = "packages/ui is build step 3 (renderer v1 — assumption ledger)";
