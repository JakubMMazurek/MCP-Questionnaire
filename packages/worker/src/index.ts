/**
 * Placeholder. The Worker (createMcpHandler + MCP SDK v2, form Durable Object,
 * ui://forms/renderer) is build step 4 per DESIGN.html §9. Nothing here yet —
 * signatures must be verified against live Cloudflare docs when it is built.
 */
export const NOT_IMPLEMENTED = "packages/worker is build step 4 (MCP handler + form DO)";
