# Local-only BASE_PATH placeholder. NOT a secret: the Worker refuses to serve
# this value anywhere but localhost (src/base-path.ts). Production uses
# `wrangler secret put BASE_PATH` with `openssl rand -hex 16`.
BASE_PATH=local-dev
